Scanner sc = new Scanner(System.in);
		
int num[] = new int[5];
int max = 0;

for(int i=0; i<5; i++) {
	System.out.println("정수를 입력하시오>>>");
	num[i] = sc.nextInt();
	if(max<=num[i]) {
		max=num[i];
	}
}
System.out.println("가장 큰 수 : "+max);
